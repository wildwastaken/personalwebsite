export { useTheme } from 'next-themes';
import { PageOpts } from 'nextra';
import { ReactNode } from 'react';

interface NextraBlogTheme {
    comments?: ReactNode;
    cusdis?: {
        appId: string;
        host?: string;
        lang: string;
    };
    darkMode?: boolean;
    footer?: ReactNode;
    head?: ({ meta, title, }: {
        meta: Record<string, any>;
        title: string;
    }) => ReactNode;
    navs?: {
        name: string;
        url: string;
    }[];
    postFooter?: string;
    readMore?: string;
    titleSuffix?: string;
}
interface BlogPageOpts extends PageOpts {
    frontMatter: Meta;
}
declare type Meta = {
    author?: string;
    back?: string;
    date?: string;
    description?: string;
    tag?: string | string[];
    title?: string;
    type?: 'post' | 'page' | 'posts' | 'tag';
};
interface LayoutProps {
    config: NextraBlogTheme;
    opts: BlogPageOpts;
}

declare const useBlogContext: () => LayoutProps;

declare function Layout(props: any): JSX.Element;

export { BlogPageOpts, LayoutProps, NextraBlogTheme, Layout as default, useBlogContext };
